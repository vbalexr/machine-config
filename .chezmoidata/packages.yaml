packages:
  # Installed via `pacman -S --needed`.
  pacman:
    - age
    - chezmoi
    - gitleaks
    - pcsclite
    - pcsc-tools
    - libfido2
    - github-cli
    - paru
    - nvm
    - dbeaver
    - discord
    - tailscale
    - claude-code
    - talosctl
    - vscodium
    - conky
  # Installed via `paru -S --needed` (not in the official repos).
  aur:
    - age-plugin-yubikey
    - yubikey-manager
    - zen-browser-bin
    - openlens-bin
    - bambu-studio

# Installed via `npm install -g` under the nvm-managed Node (no sudo).
npm_globals:
  - omnisql-mcp
