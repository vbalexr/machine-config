# General-purpose FIDO2 resident key (private key material never leaves the YubiKey).
# On a new machine, run `ssh-keygen -K` from ~/.ssh to re-download this key's public stub.
Host *
    IdentityFile ~/.ssh/id_ed25519_sk_personal
    IdentitiesOnly yes
