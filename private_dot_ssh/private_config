# General-purpose software key. Private key is stored age/YubiKey-encrypted in the
# machine-config repo and decrypted to this path by `chezmoi apply` on each machine.
Host *
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes
