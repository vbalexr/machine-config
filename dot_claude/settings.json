{
  "theme": "dark",
  "model": "sonnet",
  "effortLevel": "high",
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.message // \"Claude notification\"' | { read -r msg; url=$(cat ~/.claude/discord_webhook_url 2>/dev/null); [ -n \"$url\" ] && curl -s -X POST -H 'Content-Type: application/json' -d \"$(jq -Rn --arg m \"$msg\" '{content: $m}')\" \"$url\" >/dev/null; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
